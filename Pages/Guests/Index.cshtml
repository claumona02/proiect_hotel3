@page
@model proiect_hotel2.Pages.Guests.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Guest[0].GuestName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Guest[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Guest[0].Phone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Guest) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GuestName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
                  <td>
                    @Html.DisplayFor(modelItem => item.Phone)
                </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>

}
    </tbody>
</table>

<form method="post" asp-page="./Reservations/Index">
    <button type="submit" class="btn btn-danger">Back</button>
</form>
